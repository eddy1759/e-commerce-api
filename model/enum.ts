export enum Role {
    ADMIN = 'admin',
    USER = 'user',
    SELLER = 'seller',
    CUSTOMER = 'customer'
}

export enum PaymentStatus {
    PENDING = 'pending',
    SUCCESS = 'success',
    FAILED = 'failed'
}